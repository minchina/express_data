extends layout

block content
    h1= title
    p Welcome to #{title}
    button#showdialog.btn.btn-primary click me
    #projectDialog(title="basic dialog")
        #myfirstchart(style='height:250px')

    script(type='text/javascript').
        $(document).ready(function(){
            ToDo.main().init();
        });
